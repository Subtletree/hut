{{#if intention.bookings}}
  <thead>
    <th>Date</th>
    <th>Intention</th>
    <th>Staying at a DOC hut?</th>
  </thead>
  <tbody>
    {{#each sortedBookings as |booking|}}
      {{intentions/date-row booking=booking selected=booking.hut}}
    {{/each}}
  </tbody>
{{/if}}
