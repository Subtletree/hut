import { xNot } from 'hut/helpers/x-not';
import { module, test } from 'qunit';

module('Unit | Helper | x not');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = xNot([42]);
  assert.ok(result);
});
